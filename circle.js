var i = 0;
var y = 0;
var x = 0;

for (i = 0; i < 10; i++) {
    x + 20;
    y + 20;
    function createCircle(x, y) {
            var myCircle = new Path.Circle(new Point(x, y), 20);
            myCircle.fillColor = 'purple';
        }
}